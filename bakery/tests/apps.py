from django.apps import AppConfig


class BakeryTestsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "bakery.tests"
    label = "bakery_tests"  # Optional: a unique label
